rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read/write access for development
    // TODO: Lock down before deploying to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
